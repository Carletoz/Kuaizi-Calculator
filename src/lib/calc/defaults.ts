/** Default insurance rate: 0.35% of (FOB + freight). Industry standard approximation. */
export const DEFAULT_INSURANCE_RATE = 0.0035;

/** VUCE (Ventanilla Única de Comercio Exterior) default fee in COP */
export const DEFAULT_VUCE_COP = 39840;

/** Default IVA rate for Colombia */
export const DEFAULT_IVA_RATE = 0.19;
